services:
  - type: web
    name: google-maps-scraper
    runtime: node
    buildCommand: npm install && ./node_modules/.bin/playwright install chromium chromium-headless-shell --with-deps
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
